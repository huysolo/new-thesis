import { TopicFormButtonDirective } from './topic-form-button.directive';

describe('TopicFormButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new TopicFormButtonDirective();
    expect(directive).toBeTruthy();
  });
});
