import { DisapproveDirective } from './disapprove.directive';

describe('DisapproveDirective', () => {
  it('should create an instance', () => {
    const directive = new DisapproveDirective();
    expect(directive).toBeTruthy();
  });
});
