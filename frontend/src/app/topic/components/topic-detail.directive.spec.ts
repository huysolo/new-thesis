import { TopicDetailDirective } from './topic-detail.directive';

describe('TopicDetailDirective', () => {
  it('should create an instance', () => {
    const directive = new TopicDetailDirective();
    expect(directive).toBeTruthy();
  });
});
